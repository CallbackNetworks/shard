# Pinned exactly, like backend/requirements.txt: a floating `mcp>=1.0.0` let a
# fresh image pick up mcp 2.0, whose Server API dropped `list_tools`, so the
# server died at import while the long-running dev container kept working.
mcp==1.28.0
httpx==0.28.1
pytest==9.1.0
pytest-asyncio==1.4.0
