name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend tests
    runs-on: ci-builder
    steps:
      - uses: actions/checkout@v4

      - name: Build backend image
        run: docker compose -f docker-compose.ci.yml build backend

      - name: Lint
        run: docker compose -f docker-compose.ci.yml run --rm backend ruff check app/ tests/

      - name: Format check
        run: docker compose -f docker-compose.ci.yml run --rm backend ruff format --check app/ tests/

      - name: Run tests with coverage
        run: docker compose -f docker-compose.ci.yml run --rm backend pytest tests/ -v --tb=short --cov=app --cov-report=term-missing --cov-fail-under=70

      - name: Security audit
        run: docker compose -f docker-compose.ci.yml run --rm backend sh -c "pip install pip-audit && pip-audit --strict"

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v

  frontend:
    name: Frontend tests
    runs-on: ci-builder
    steps:
      - uses: actions/checkout@v4

      - name: Build frontend image
        run: docker compose -f docker-compose.ci.yml build frontend

      - name: Lint
        run: docker compose -f docker-compose.ci.yml run --rm frontend npm run lint -- --max-warnings 300

      - name: Run tests
        run: docker compose -f docker-compose.ci.yml run --rm frontend npx vitest run

      - name: Security audit
        run: docker compose -f docker-compose.ci.yml run --rm frontend npm audit --audit-level=moderate

      - name: Build
        run: docker compose -f docker-compose.ci.yml run --rm frontend npx vite build

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v

  integration:
    name: Integration smoke test (prod)
    needs: [backend, frontend]
    runs-on: ci-builder
    steps:
      - uses: actions/checkout@v4

      - name: Build production images
        run: docker compose -f docker-compose.ci.yml --profile integration build backend-prod frontend-prod

      - name: Start services
        run: docker compose -f docker-compose.ci.yml --profile integration up -d backend-prod frontend-prod

      - name: Wait for backend health
        run: |
          for i in $(seq 1 30); do
            curl -sf http://localhost:8000/health && exit 0
            sleep 2
          done
          echo "Backend health check failed"
          docker compose -f docker-compose.ci.yml --profile integration logs backend-prod
          exit 1

      - name: Smoke test frontend
        run: curl -sf http://localhost:80/ > /dev/null

      - name: Cleanup
        if: always()
        run: docker compose -f docker-compose.ci.yml --profile integration down -v
